# 换机迁移清单 — 华泰风险平价策略仓库

> 生成时间：2026-09-02　｜　基于本机实际依赖核查

## 一句话结论

**复制项目文件夹能解决约 90% 的问题，剩下 10% 是运行环境，需要新机器另行安装。**

⚠️ **绝对不要只从 GitHub clone** —— 见下方「被 gitignore 的关键文件」，远端是残缺的。

---

## 一、直接复制的部分（项目文件夹，约 241M）

**源路径**：`C:\Users\aa\WorkBuddy\2026-05-28-14-41-45\huatai-risk-parity-replication`

整个文件夹打包复制即可，包含：
- `.git/`（107M，完整提交历史，**含尚未推送的 4 个提交**）
- 策略代码、回测脚本、数据 CSV
- `.workbuddy/`（自动化任务配置、memory 记录）
- 所有被 gitignore 的文件（见下）

### 被 gitignore、但迁移必须带走的文件

这些在 GitHub 上**没有**，只有本地有，漏了就跑不起来或丢历史产出：

| 文件/目录 | 内容 | 漏了的后果 |
| --- | --- | --- |
| `.env` | JYDB 连接配置，**含数据库密码** | 无法连接数据库，数据更新直接失败 |
| `策略复现与回测/每日更新策略/输出/` | 全部回测报告、净值曲线、仓位明细、图表 | 丢掉所有历史日报与净值序列 |
| `数据/日度收益数据更新/增量缓存/*.csv` | 数据更新增量缓存 | 需重新全量拉取 |

---

## 二、文件夹之外，新机器必须另行准备

### 1. Python 环境（不要复制 venv）

- 本机 venv 在 **项目外**：`C:\Users\aa\.workbuddy\binaries\python\envs\default`
- 该 venv 里装了大量与本策略无关的包（akshare、easyocr、faster-whisper 等）
- **不要直接复制 venv 文件夹** —— 内含绝对路径，换机后必坏

新机器做法（以 Python 3.13 为例）：
```bash
python -m venv <新路径>/envs/default
<新路径>/envs/default/Scripts/python.exe -m pip install -r requirements.txt
```

`requirements.txt` 只需 6 个包：
```
pandas==2.3.1
numpy==2.3.2
matplotlib==3.10.8
scipy==1.17.0
openpyxl==3.1.5
pyodbc==5.3.0
```

### 2. ODBC Driver 17 for SQL Server（必需）

pyodbc 靠它连 JYDB，不装则数据更新报错。
- 本机已确认安装：`ODBC Driver 17 for SQL Server`
- 新机器需从微软官网下载安装 `msodbcsql17.msi`

### 3. 数据源：可替换（当前为 JYDB）

- 当前数据源：JYDB，地址 `192.168.10.48:1433`，本机连通性 ✅ 可达
- **数据源可替换**——换机后若内网不可达，改配置指向其他数据库即可，无需改代码

⚠️ **但有个前提**：脚本依赖的是 **JYDB（聚源）的表结构**，具体用到 5 张表：

| 表名 | 用途 |
| --- | --- |
| `dbo.Fut_TradingQuote` | 金融期货（IF/IC/IM/T/TL）行情 |
| `dbo.Fut_DailyQuote` + `dbo.Fut_ContractMain` | 商品期货（AU/M/CU/AL/TA/SC）行情与主力合约 |
| `dbo.DZ_DailyQuote` | 红利低波 ETF（512890.SH，InnerCode=201577） |
| `dbo.QT_IndexQuote` | 中证1000 指数（IC 上市前收益回退） |
| `FTDB.dbo.ths_GC` | GC001 一天期国债逆回购利率 |

因此分两种情形：

**情形 A：换一个同构的聚源 JYDB（推荐，零代码改动）**
只要对方库里有上述同名的表和字段，改 4 个配置项即可，脚本一行都不用动。

**情形 B：换完全不同厂商（Wind / 通联 / Tushare 等）**
schema 不一样，需要重写 `日度收益数据更新.py` 的取数逻辑 + `数据源查询模板.sql` 的品种映射（涉及 10 个品种的交易所代码、品种内部编码、主力合约判定、前复权处理）。这是**数天级别的改造**，不是配置项问题。

> 结论：换机首选情形 A。情形 B 属于新需求，建议换机完成后单独排期。

#### 切换数据源怎么改（情形 A，两种方式任选）

**方式一：改 `.env` 文件（推荐，一劳永逸）**

`.env` 中只有 4 个相关变量，改完即刻生效：

```ini
JYDB_SERVER=新数据库地址      # 例如 10.0.0.5 或 新地址,1433
JYDB_DATABASE=JYDB
JYDB_UID=新用户名
JYDB_PWD=新密码               # 脚本不保存密码，只从此处读取
JYDB_DRIVER=ODBC Driver 17 for SQL Server
```

**方式二：命令行参数覆盖（临时测试用）**

脚本支持 5 个参数，优先级高于 `.env`：

```bash
python 数据/日度收益数据更新/日度收益数据更新.py ^
  --jydb-server 新地址 ^
  --jydb-database JYDB ^
  --jydb-uid 用户名 ^
  --jydb-password 密码
```

**改完怎么验证**：项目里已内置连接探测脚本，跑它会打印连接结果和各张表的最新交易日：

```bash
python 数据/日度收益数据更新/_conn_probe.py
```

看到 `CONNECT = OK` 且能打印出各品种最新日期，就说明新数据源接上了。

### 4. WorkBuddy 与定时任务（按需）

若要在新机器继续跑每日 15:30 的自动更新：
- 安装 WorkBuddy 客户端
- 自动化配置在 `.workbuddy/automations/` 内（随文件夹复制），但需在客户端中重新注册/启用

### 5. 用户级记忆（可选）

跨项目偏好记录在 `C:\Users\aa\.workbuddy\MEMORY.md`，如需一并迁移请单独复制。

---

## 三、迁移后自检（按顺序跑一遍）

1. `git log --oneline -5` —— 确认提交历史完整（应含未推送的 4 个提交）
2. `git status` —— 确认工作区干净
3. `python -c "import pandas, pyodbc"` —— 确认环境与 ODBC 模块正常
4. 运行一次 `策略复现与回测/每日更新策略/daily_update_strategy.py` —— 确认能连 JYDB 取数并出报告
5. 检查 `输出/报告/` 下是否生成了新日期的报告

---

## 四、关于 Git 远程

- 截至 2026-09-02，本地有 **4 个提交未推送**（网络链路故障，push 一直 502）
- 远程停在 `1a22077`
- 因为 `.git` 会随文件夹一起复制，**未推送的提交不会丢**，换机后网络恢复再推即可
- 不影响本地策略运行（策略链路不依赖 git，详见 memory 记录）
