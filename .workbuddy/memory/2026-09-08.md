# 2026-09-08 工作日志

## 每日策略更新（手动触发「更新数据」）
- 运行 `策略复现与回测/每日更新策略/daily_update_strategy.py`，数据更新至 **2026-09-07**（上次 2026-09-02，跨多个交易日，属新数据）。
- v0.19 核心指标：期末净值 2.5974、年化 12.10%、年化波动 6.55%、夏普 1.78、最大回撤 -7.79%、日胜率 56.51%、平均资金占用 15.59%。
- 近期表现：上一交易日 +0.17%、近1月 +0.84%、近3月 +0.69%、YTD +5.90%（较 09-02 全线回升）。
- 股指期货信号仍为 0.70 → 股指总仓位 21%（IF/IC 各 10.5%）。
- 10 大持仓：10年国债 53.81%、沪深300 10.50%、中证500 10.50%、豆粕 6.15%、红利低波ETF 4.63%、沪铜 4.49%、沪铝 4.52%、沪金 2.31%、PTA 1.83%、原油 1.25%。
- **股指信号到期提醒**：最后人工填值 2026-08-27，距今 **12 天**，逼近 15 天上报阈值；本次仍维持 21% 仓位（ffill）。再超 3 天未更新应主动提示补录。
- Git：本地提交 `adaae79`（已 `git add -A`，10 文件变更），但 **`git push origin main` 失败**：`Recv failure: Connection was reset` / `Failed to connect to github.com:443 ... Could not connect to server`（GitHub 443 链路临时故障，与 09-02 同性质）。`git ls-remote` 也无法连，确认是网络侧故障。
- 处理：按规则不阻塞，本地提交保留；网络恢复后需手动 `git push origin main`。下次执行「更新数据」时优先重试 push。
- **续推重试（08:40 后）**：「请继续工作」后再试 push，连续 3 次仍 `Recv failure: Connection was reset` / 443 超时，`git ls-remote` 亦连不上。确认 GitHub 链路持续故障，非本地问题。本地待推送仅 `adaae79` 一笔，工作区干净。暂挂起，网络恢复后手动 `git push origin main` 即可。
