# 2026-09-23 工作日志

## 13:22 手动触发「更新程序」（用户指令）
- 运行 `daily_update_strategy.py`（venv python），数据日期由 2026-09-21 推进至 **2026-09-22**，报告 `回测报告_2026-09-22.md`。本次重算**应用了 09-22 信号 0.5**（此前 09-18~09-21 为 ffill 0.3）。
- 核心指标(v0.19)：净值 2.6032 / 年化 12.06% / 波动 6.54% / 夏普 1.78 / 回撤 -7.79% / 日胜率 56.50%；股指信号 0.50→仓位 **15.00%**（沪深300/中证500 各 7.50%，较此前 0.3→4.50% 各 +3pct）。
- 近1月 +0.87% / 近3月 +0.45% / 年初至今 +6.14%。10年国债降至 61.74%（释放权重给股指）。

## ⚠️ ODBC 驱动故障与修复（13:22 跑批初次失败）
- 初次运行报 `pyodbc.InterfaceError: ('IM002', 未发现数据源名称)`：`.env` 与脚本默认均写 `ODBC Driver 17 for SQL Server`，但本机注册表**已无该驱动**（仅剩旧 `SQL Server` 32/64 位）。09-21/09-22 能跑说明当时驱动 17 还在，现已丢失。
- 验证：旧 `SQL Server` 驱动可连 JYDB（连得上，最新 TradingDay=2026-09-22），且兼容连接串中的 `Encrypt=no` 属性。
- **修复**：将仓库 `.env` 的 `JYDB_DRIVER` 由 `"ODBC Driver 17 for SQL Server"` 改为 `"SQL Server"`（脚本经 `load_env_file` 自动注入 `.env`，手动与每日自动化均沿用）。`.env` 不入库，属本地配置调整。
- 用 `JYDB_DRIVER="SQL Server"` 重跑成功（22s）。**明早 09:00 自动任务将沿用此驱动，不再报 IM002。**

## git 同步
- 已提交 `b07e895`（含 09-22 数据层更新 + 信号 xlsx 09-22=0.5 变更 + memory）。本地此前还积压 09-21 的 `5aada4d` 未推送。
- `git push origin main` **成功**：`7cd86ac..b07e895`，两个积压提交（09-21 + 09-22）全部同步至远程，远程现为 `b07e895`。

## 13:39 09:00 自动化任务执行（本次延迟触发）
- 运行 daily_update_strategy.py：数据日期仍为 **2026-09-22**（今日 13:22 手动已更新至该日），脚本 data_update=updated，重算完成。
- 核心指标(v0.19)：净值 2.6032 / 年化 12.06% / 波动 6.54% / 夏普 1.78 / 回撤 -7.79% / 日胜率 56.50%；信号 0.50→仓位 15.00%。
- 提交 `a4ddc9d`（数据层 + memory），`git push origin main` **成功**：`b07e895..a4ddc9d`。
- 顺带完成 MEMORY.md 精简合并（超 3000 字符上限，去除冗余历史细节，保留全部运维关键事实）。
