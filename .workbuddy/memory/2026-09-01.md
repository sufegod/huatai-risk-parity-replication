# 2026-09-01 工作日志

## 每日策略更新自动化（15:30）诊断
- 自动化跑 daily_update_strategy.py：数据日期仍为 2026-08-31，与上次提交相同 → 判定「今日无新数据，策略未重算」，未提交/推送。
- 用户追问是否数据库连不上。经排查：**JYDB（192.168.10.48, tsreadonly）连通正常**。
- 根因：脚本结束日取「各数据源最新日期的最小值」。探针显示 2026-09-01（周二）收盘后，沪金主连、红利低波ETF 已入库 09-01，但沪深300主连、10年国债主连、GC001 仍停在 2026-08-31 → 共同最新日 = 2026-08-31，其余源跳过、0 增量。属数据供应商入库延迟，非故障。
- 结论：非交易日 / 源端部分数据未齐导致，等各源补齐 09-01 后下次运行即可正常更新。
- 临时探测脚本 `数据/日度收益数据更新/_conn_probe.py` 已删除。

## 自动化调度调整（用户要求）
- **每日策略更新时间：15:30 → 每天 09:00**（automation-1787648434423）。理由：数据供应商在收盘后数小时才入库，15:30 跑常撞上数据未齐；改到次日上午可拿到完整前一交易日数据，也避开 17:30 关机。
- **新增一次性任务**：`ee888eb7-9099-41f4-853f-1f3d5b4863e0`「检查9月1日数据补齐情况」，2026-09-02 09:30 执行。定在 9:30 是为了避开与 9:00 日常任务并发跑同一脚本（会争抢 CSV 写入）。它会逐个资产探测 JYDB 最新日、汇报哪些源已到 09-01；若已补齐而 9 点那次未重算则补跑策略+提交+推送。
- 另两个任务未动，但提醒过用户：`ma60` 每天 18:00、`a` 每天 17:30，均落在 17:30 关机之后，实际可能跑不到。
- 用户回复「ma60 和 A股追踪这两个任务暂时不用更新了」，语义有歧义（可能=不要改时间，也可能=停用任务）。已用选项框确认但未收到选择 → **保守处理：两个任务保持 ACTIVE、时间不动**。等用户明确「暂停」再执行（暂停可恢复，但停用会漏一天数据，故不擅自动手）。
