# 项目长期记忆 — huatai-risk-parity-replication

## 仓库 Git 环境（2026-09-02 确认）
- **坑：本地远程追踪引用 `refs/remotes/origin/main` 已失效**，停在旧值 `8a54e75`（2026-05-28），且 git 无法写入更新（`git fetch` 打印成功、`git update-ref` 返回 0，但引用不变；`.git/refs/remotes/origin/` 目录会被清掉）。
- **后果**：`git status -sb` 会虚报 `ahead 29/30`，这是**幽灵领先**，不代表真的有那么多提交没推。
- **正确做法**：用 `git ls-remote origin` 取远程真实 SHA，再 `git log --oneline <远程SHA>..main` 看真实待推送提交。不要相信 `origin/main`。
- **GitHub 链路不稳定**：push 常报 `CONNECT tunnel failed, response 502` 或 `schannel: server closed abruptly (missing close_notify)`；严重时连 `ls-remote` 也 502（透明代理故障，git config 与 env 均未配代理）。属临时性故障，换个时间重试往往能成功（8/31 曾成功推送）。
- **失败呈「同日间歇性」（2026-09-11 实测）**：同一天 10:13、10:51 两次 push 失败（`Recv failure: Connection was reset` / `Failed to connect to github.com:443`），11:25 原样重试即成功且仅 13 秒推完 3 个提交。⇒ **遇失败不要改 git 配置**（改 `http.postBuffer`/`http.version` 无必要，反而留下残留配置），隔几十分钟原样重试即可；大文件（19MB csv）在慢链路下 push 可能挂起 20+ 分钟才报错，属正常现象。
- **推送前必做**：`git ls-remote origin` 取远程真实 SHA → `git log --oneline <远程SHA>..main` 列真实待推送提交，据此汇报积压数量（避免只凭猜测说「1 个提交」）。
- GitHub SSH(443) 网络可达，但本机**未配置 SSH key**（`Permission denied (publickey)`），暂不能作为 https 的备份通道。

## ⚠️ 本机 Shell PATH 损坏的绕行方案（2026-09-14 起，重要）
- **症状**：Bash 工具报 `dirname: command not found`、`cd: null directory`，`tr`/`head`/`tail`/`git` 等全部 127 找不到；PowerShell 工具 stdout 被吞（命令显示 exit 0/1 但无输出）。Python 仍可直接用绝对路径运行。
- **根因**：工作区 shell 的 PATH 环境变量丢失，非仓库问题。
- **✅ 绕行（已验证可用）**：所有 git/需要 coreutils 的命令，一律走 PortableGit 的 **login shell**（`-l` 会自行加载 PATH）：
  ```
  /c/Users/aa/.workbuddy/binaries/PortableGit/versions/1.2.0/bin/bash.exe -lc "cd <repo> && git ... "
  ```
- **配套注意**：
  - 运行 Python 脚本并在 Bash 里看中文输出，须加 `PYTHONIOENCODING=utf-8`，否则中文乱码/无输出。
  - 用 Python 脚本（`Write` 写 .py 再跑）比在命令行内联多行脚本更可靠，避免引号与编码问题。
  - **push 等长时间操作必须用 `run_in_background=true`**：慢链路推 19MB csv，前台 400s 会 SIGTERM 中断，后台则正常完成。
  - 不要把 `tr -d '\r'` 用在 Bash 管道里（`tr` 恰好不可用）。
- **恢复**：重启 IDE/终端通常可恢复 PATH；未恢复前按上述方式操作。

## 策略与流程约定
- 「更新策略」= 运行 `策略复现与回测/每日更新策略/daily_update_strategy.py`（venv: `C:/Users/aa/.workbuddy/binaries/python/envs/default/Scripts/python.exe`），更新数据至最新交易日 + 重算回测。
- 每日 **09:00** 自动化任务（automation-1787648434423，2026-09-22 由 9:00 短暂改 9:30 后用户明确改回 9:00「开盘就更新持仓调整」）：更新 → 读报告 → add/commit → 尝试 push；push 失败仅汇报不阻塞。
- 当前策略版本 v0.19（IC 替换 IM + 剔除 30 年国债 TL，10 年国债权重约 53%）。
- 报告输出目录：`策略复现与回测/每日更新策略/输出/报告/回测报告_YYYY-MM-DD.md`（另有 仓位/净值/指标/仓位明细/图表 子目录）。
- 报告默认只列 top 8 持仓；需要完整 10 大持仓时读同日期的 `输出/仓位/仓位_YYYY-MM-DD.csv`。

## 每日产物的存储格式（2026-09-02 确认）
- **每日更新不生成任何 Excel**，全部是 **CSV**（两个核心脚本 `daily_update_strategy.py` 与 `日度收益数据更新.py` 均不含 `to_excel`/`ExcelWriter`）。
- **数据层（覆盖式，全量集中在一个文件，每日追加新日期后覆盖）**：`数据/日度收益数据更新/` 下的 `日涨跌幅_填充.csv`、`日涨跌幅_未填充.csv`、`期货主力前复权收盘价.csv`，以及 `增量缓存/` 下的 `期货行情.csv`(19M)、`中证1000指数行情.csv`、`红利低波ETF行情.csv`、`GC001.csv`。
- **策略产物（按日期新建文件，不覆盖，历史全部保留）**：`输出/净值/策略每日净值走势_YYYY-MM-DD.csv`、`输出/仓位/仓位_YYYY-MM-DD.csv`、`输出/仓位明细/策略日度仓位明细_YYYY-MM-DD.csv`、`输出/指标/年度及全局回测指标_YYYY-MM-DD.csv`、`输出/报告/回测报告_YYYY-MM-DD.md`、`输出/图表/回测图表_YYYY-MM-DD.png`。
- 仓库内仅存的 xlsx 均为静态原始数据或手工维护文件，**不随每日更新变化**。

## ⚠️ 运维盲区：股指期货信号需人工维护
- `数据/原始数据/股指期货信号.xlsx` 是策略的**输入**（唯一被策略读取的 xlsx），由 `load_index_signal()` 读取，**不是每日自动生成的产物**。
- 加载逻辑（见 `策略复现与回测/策略代码/资产风险平价策略0.19（IC替换IM+日频胜率）.py` 第 165-186 行）：定位含"股指期货"的列 → 转数值 → **`.ffill()` 前向填充**。
- **风险**：该文件需人工填值。2026-08-27 填入 0.7 后，至 2026-09-01 后续日期全为 NaN，策略靠 ffill 一直沿用 0.7 → 股指仓位恒定 21%。**信号长期不变并非市场无变化，而可能是忘了填新值。**
- **排查方法**：读该 xlsx 尾部，检查最后一条有效值距今天数。若长期未更新，需人工补录新信号值。

### 信号 → 仓位 的映射公式
`INDEX_BASE_WEIGHT = 0.30`（常量，见 v0.19 策略第 40 行）
```
股指总仓位 = 0.30 × normalize(signal)
normalize: signal <= 0 或 NaN → 0；否则 min(signal, 1.0)
```
算例：signal=0.7 → 0.30×0.7 = **21%**（IF/IC 各半，即各 10.5%）；signal=1.0 → 30% 满配；signal=-1.0 或 0 → **清仓股指**。
即值域 [-1,1] 的主观打分线性映射到 [0,30%] 仓位，负值等同空仓。

### 信号的统计特征（判断"该不该催更新"的依据）
- 有效值 2067 个，覆盖 2018-01-02 ~ 2026-09-10（**2026-09-11 补录**：原 2053 个/截至 08-27；08-28~09-10 已显式填 0.70）。
- **取值高度离散，仅 8 个唯一值**：1.0(975次)、-1.0(705次)、0.0(287次)、0.5(43次)、0.7(39次)、0.25(2次)、0.3(1次)、0.35(1次)。属**人工档位打分**，非连续指标。
- **更新节奏不规律**：2026 年各次更新间隔约 1~21 天，**平均约 10 天**（例：08-06→08-13 隔7天，08-13→08-27 隔14天）。
- ⇒ 判断标准：距上次更新 **<10 天属正常**；**>15 天**应主动提醒补录；若超过 20 天仍沿用旧值，需确认是否遗漏。
- 注：信号为主观判断（宏观/政策/情绪综合），**无法用规则化指标自动推导**，自动化方向只能是"提醒"而非"代算"。

### 用户决策记录：股指信号变更轨迹（人工维护输入）
- 信号文件 `数据/原始数据/股指期货信号.xlsx` 为人工维护输入，**仅在变更日填值、其余 ffill 延续**；留空 ≠ 漏填（维护规则见下）。
- **变更轨迹**：08-27=0.7（维持至 09-15）→ 09-16 用户改 0.5、09-17 改 0.3（09-18~09-21 经 ffill 延续 0.3）→ **09-22 用户改为 0.5**（新增一行，09-22 及以后生效；09-18~09-21 仍 0.3，待 09-22 数据落库重算后切换为 0.5→15% 仓位）。
- 每日更新汇报**不再反复提示"信号陈旧"**；仅在信号实际变更或用户主动提出时汇报。
- 触发调整的条件：用户主观判断（宏观/政策/情绪），需人工填录到 xlsx 变更日那一行。

### 信号文件维护规则（2026-09-11 用户明确）
> 用户原话：**「逻辑很简单，只要不更改股指期货信号则延续之前的信号」**。
- ⇒ **该文件只需在"信号发生变更"的那一天填值**；其余日期留空是**正常且推荐**的做法，策略会自动延续上一有效值。**留空 ≠ 漏填**。
- 代码依据：`load_index_signal` 做 `.ffill()`（策略 v0.19 第 186 行）→ 使用处 `index_signal.reindex(df_trade.index, method='ffill')`（第 271 行）。因此即使文件末尾之后没有行，后续交易日仍延续最后一个有效值。
- **后续操作约定**：用户给出新信号时，**只在当天（变更日）新增一行填新值**即可，**无需**把中间空白日期逐日补齐；不要因为看到大片空白就提示"漏填"。
- 映射：信号 ≤0 → 清仓股指；信号 ∈(0,1] → 仓位 = 30% × 信号。算例：0.70→21%（IF/IC 各半）、0.50→15%、0.30→9%。
- 说明：2026-09-11 那次「逐日补录 08-28~09-10」为可选文档化操作（结果与 ffill 完全一致），**今后不必重复此做法**。
  - **2026-09-11 更新：已完成显式补录**。用户经确认后，将 08-28 ~ 09-01 的空白单元格填为 0.70，并追加 09-02 ~ 09-10 共 9 行（同为 0.70）。**此后不再依赖 ffill 延续**，文件本身即可看出"有意维持 0.70"。
  - 补录原则：只填空白 + 追加缺失行，**未改动任何历史有效值**（已与备份逐格比对，日期列 0 差异，仅 5 处 None→0.7 为预期变更）。
  - 端到端验证：补录后重跑策略，指标与补录前完全一致（净值 2.6074 / 夏普 1.78 / 信号 0.7000 / 仓位 21.00%）⇒ 补录为纯文档性操作，**不影响策略结果**。
  - 备份：`C:\Users\aa\WorkBuddy\2026-05-28-14-41-45\_backup_股指期货信号_20260911.xlsx`（仓库外，38119 字节，原修改时间 2026-08-28 15:06）。
  - 该文件「只在信号变更日填值、其余留空」的固有风格在 08-27 之前仍然保留（如 08-13=0.5 后留空至 08-27=0.7），本次未做改动。

## 仓库 git 忽略规则（2026-09-10 确认）
- `.gitignore` 第 33 行忽略了 **`策略复现与回测/每日更新策略/输出/`** ⇒ 每日生成的 **报告/图表/净值/仓位/指标/仓位明细 全部是本地文件，不入库、不随 push 同步**。
  - 这解释了「每日提交只有 9 个文件」：数据层 7 个（含增量缓存中已跟踪的 csv） + memory 2 个，**不含任何输出产物**。
  - 因此每日报告/图表需在**本机**查看；换机或需异地访问时，必须另行拷贝。
  - 注意：`数据/日度收益数据更新/增量缓存/*.csv` 虽在 .gitignore 中，但已被跟踪，故修改仍会出现在提交里（gitignore 只对未跟踪文件生效）。

## 策略文件沿革与清理记录
- 真实创作时间以**文件系统创建时间**为准；`git log` 因批量入库（如 08-31 的 `1a22077`）会把多日产物压成同一提交，**不可用于判断创作日**（曾误判）。
- **2026-09-10 已删除**：2026-08-28 下午创建的全部「趋势择时 v0.20 系列」共 20 个文件（4 脚本 + 16 回测产物，后缀 `_v0.20_trend/_15d/_30d/_60d`），提交 `aeeba27`。仓库外留有可恢复副本：`C:\Users\aa\WorkBuddy\2026-05-28-14-41-45\_deleted_trend_timing_backup_2026-09-10\`。
- **未删除（边界外）**：`商品趋势模型`（命名"趋势模型"非"择时"，仍保留）；`历史策略版本/资产风险平价策略0.08（全天候增强策略：动量择时）.py`（2026-06-03，早于8月中旬）。
- 现存策略脚本谱系：v0.16/0.17（05-28）、0.18（06-05）、**0.19（06-09，当前生产版）**；测试脚本 0.18_1、0.19_1、商品趋势模型回测_v0.1。

## 数据源与连接链路（2026-09-10 查证）
**核心链路：JYDB 数据库 → 更新脚本 → 本地 CSV → 策略（策略本身不连库）**
- **数据库**：SQL Server 内网实例 **`192.168.10.48`**（默认端口 1433），同一实例上有两个库：
  - **`JYDB`**（聚源数据库，主库）—— 期货主力行情 `Fut_TradingQuote`、指数 `QT_IndexQuote`、ETF（`512890.SH`）等。
  - **`FTDB`** —— 一天期国债逆回购 GC001：`FTDB.dbo.ths_GC`，代码 `204001.SH`，字段 `ths_wgt_avg_interest_bbond`。
- **连接方式**：Python `pyodbc`；账号 **`tsreadonly`（只读）**；连接串含 `Encrypt=no;TrustServerCertificate=yes;`。
- **⚠️ ODBC 驱动坑（2026-09-23 实测）**：本机**已无「ODBC Driver 17 for SQL Server」**（注册表只余旧的 `SQL Server` 32/64 位驱动）。脚本默认/`.env` 原写 17 → 跑批报 `IM002 未发现数据源名称`。**修复**：`.env` 的 `JYDB_DRIVER` 已改为 `"SQL Server"`（旧驱动实测可连 JYDB、且兼容 `Encrypt=no` 属性）。脚本经 `load_env_file` 自动注入 `.env`，手动与每日自动化均沿用此驱动。若日后重装 17/18 可改回，但当前环境只能用 `SQL Server`。
- **密码不落盘**：从**仓库根目录 `.env`** 读取 `JYDB_PWD`（另有 `JYDB_SERVER/DATABASE/UID/DRIVER`）。`.env` 还含 `IFIND_MCP_URL`/`IFIND_MCP_AUTHORIZATION`（iFinD 残留配置，每日更新脚本已不再使用 iFinD）。
- **唯一连库入口**：`数据/日度收益数据更新/日度收益数据更新.py`（另有 query_ic500.py / debug_ic500.py / compare_ic500_vs_im1000.py / _conn_probe.py 等调试脚本走同一实例）。
- **策略只读本地文件**：v0.19 读取 `数据/日度收益数据更新/日涨跌幅_填充.csv`、`日涨跌幅_未填充.csv`、`数据/原始数据/股指期货信号.xlsx`。
- **另有一条独立数据源**：新浪财经（akshare `futures_main_sina` / `fund_etf_hist_sina`），仅用于**商品趋势模型**，与原全天候策略无关。

## 商品趋势模型的数据隔离副本（2026-09-10 建立）
- **位置**：`商品趋势模型/历史数据/`（+ `增量缓存/` 子目录），附 `README.md` 说明。
- **来源**：从 `数据/日度收益数据更新/` 单向保真复制（`cp -p`）7 个文件——`期货主力前复权收盘价.csv`(核心)、`日涨跌幅_填充.csv`、`日涨跌幅_未填充.csv`，及 `增量缓存/{期货行情,中证1000指数行情,红利低波ETF行情,GC001}.csv`。
- **动因**：此前 `策略复现与回测/策略测试代码/商品趋势模型回测_v0.1.py` **直接引用原策略数据**，违反「新策略必须与原策略物理隔离」的铁律。现已把该脚本 `PRICE_FILE` 指向副本（原路径保留为注释）。
- **同步规则**：单向 **源 → 副本**，需要刷新时重新 `cp -p`；**严禁反向回写**，原策略数据始终是唯一权威源。原策略任何文件不得因新策略而变动。
- **体积注意**：副本含 `增量缓存/期货行情.csv`（19MB）。当前未被 gitignore（可入库），入库会明显增大仓库体积。


